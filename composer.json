{
  "name": "mon-projet-email",
  "description": "Un projet PHP pour envoyer des emails avec Gmail SMTP",
  "require": {
    "phpmailer/phpmailer": "^6.6",
    "vlucas/phpdotenv": "^5.4"
  },
  "scripts": {
    "post-install-cmd": [
      "php -r \"copy('.env', '.env');\""
    ]
  }
}
